name: Print Message
on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print a Message
        run: echo "Hello! A Push detected on Main Branch"

      - name: Print a Second Message
        run: echo "This is the second message"